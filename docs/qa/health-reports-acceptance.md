# 健康报告独立验收记录

日期：2026-09-10

QA 分支：`codex/reports-qa`

独立工作树：`E:\Healthy-worktrees\reports-qa`

## 共同基线

- 精确基线：`88388d2acb197c9f16797863d9f0fdb1554348c5`
- 后端候选：`4b3ddd4435d2bf56e644dde909ff3961be0d1fd3`
- Flutter 候选：`b2fc55106c8daf41777966e24a406a46a59ab010`
- 合并提交：后端 `3fdac13a6ac621127049dbae342d20d9322f18ac`；Flutter `b37476503118d67ec4401e1930769095ec48dc78`
- QA 最终候选：本文件所在的最终 QA 提交
- Git 邮箱：`3040503900@qq.com`

本文件先于产品候选建立。第一阶段只记录未合并候选的共同基线；候选合并、独立回归、Android 触控、PDF 实测及最终门禁将在任务 15–16 阶段回填。

## 环境与未合并候选基线

| 项目 | 环境或命令 | 结果 |
|---|---|---|
| Java | OpenJDK `17.0.19` | 可用 |
| Flutter / Dart | Flutter `3.47.2`、Dart `3.13.2` | 可用 |
| ADB | `1.0.41`（platform-tools `37.0.1-15733141`） | 可用 |
| 模拟器 | AVD `LightBite_API_36` | 已安装，基线时未在线；不阻断并行开发 |
| 数据库 | 本机仅 MySQL `5.7.44`，Docker 服务不可用 | MySQL 8.4 不可用；保留为发布前阻断门禁 |
| 后端全量 | `backend\.\mvnw.cmd test` | 通过：141 项，0 失败、0 错误、0 跳过 |
| Dart 格式 | `dart format --output=none --set-exit-if-changed lib test` | 通过：94 个文件，0 个变化 |
| Flutter 静态分析 | `flutter analyze` | 通过：`No issues found` |
| Flutter 全量 | `flutter test` | 通过：168 项 |
| Debug APK | `flutter build apk --debug` | 通过：`E:\Healthy-worktrees\reports-qa\mobile\build\app\outputs\flutter-apk\app-debug.apk`；179,874,512 字节；SHA-256 `487571A8D855D3313921A061C75BCAC04AA0AA4185B614429C95B137DC8A9EB1` |
| 仓库总检查 | `.\scripts\check.ps1` | 通过：`All project checks passed`，其中后端 141 项再次通过 |
| 空白与状态 | `git diff --check`、`git status --short --branch` | 通过；提交前仅本验收文档未跟踪 |

非阻断基线提示：H2 `2.4.240` 高于 Flyway 已验证版本 `2.3.232`；11 个 Flutter 包有与当前约束不兼容的新版本；`flutter_timezone` 仍使用 Kotlin Gradle Plugin；Android Studio 与命令行 SDK XML 版本存在工具链提示。以上均未造成测试、分析或 APK 构建失败。

状态说明：`[ ]` 为等待候选或设备验证；`[x]` 只有在记录自动化、人工审查或设备证据后才能勾选。MySQL 8.4 未实测时始终保持未勾选，并明确作为部署前阻断门禁。

## 数据库迁移矩阵

| 状态 | 验收项 | 验证层级 | 证据 |
|---|---|---|---|
| [ ] | 空库 V1→V11，报告表、来源表、外键、唯一约束、检查约束和索引正确 | 迁移 | `ReportMigrationTests` |
| [ ] | V10→V11 保留账号、档案、计划及六类健康数据 | 迁移 | 升级前后逐表行值断言 |
| [ ] | 同用户、类型、周期、版本唯一；同用户幂等键唯一 | 迁移/服务 | 串行与并发测试 |
| [ ] | 来源引用按报告、指标与来源组合去重，软删除报告不删除源数据 | 迁移/服务 | 数据库断言 |
| [ ] | 快照使用兼容文本字段，不保存 PDF 二进制或公开地址 | 迁移/隐私审查 | schema 与内容扫描 |
| [ ] | MySQL 8.4 执行 V1→V11、V10→V11、备份和恢复演练 | MySQL 8.4 发布门禁 | 当前环境不可用，发布前阻断 |

## 周期与时区矩阵

| 状态 | 验收项 | 验证层级 | 证据 |
|---|---|---|---|
| [ ] | 日报按请求 IANA 时区自然日计算 | 纯单元/HTTP/API 36 | 日界与 UTC 边界断言 |
| [ ] | 周报固定周一至周日，覆盖跨月、跨年 | 纯单元/服务/API 36 | 周边界参数化测试 |
| [ ] | 月报固定自然月，覆盖闰日、月末和跨年 | 纯单元/服务/API 36 | 月边界参数化测试 |
| [ ] | 当前周期为 `IN_PROGRESS`，历史周期为 `COMPLETE` | 纯单元/Widget/API 36 | cutoff 与页面标记 |
| [ ] | 当前周期覆盖天数只计算 cutoff 前已过去本地日期 | 纯单元/服务 | 充分性边界测试 |
| [ ] | 上一周期始终取上一完整自然日/周/月 | 纯单元/服务 | 对比周期断言 |
| [ ] | DST 缺失/重复时刻不按固定 24 小时切分 | 纯单元/服务 | 夏令时参数化测试 |
| [ ] | 无效时区、未来周期为中文 400，不回退服务器时区 | HTTP/Dart | 错误信封测试 |

## 充分性与指标矩阵

| 状态 | 验收项 | 验证层级 | 证据 |
|---|---|---|---|
| [ ] | 六个栏目分别返回 `SUFFICIENT/LIMITED/EMPTY/NOT_APPLICABLE` 和中文原因 | 纯单元/Dart/Widget | 四态覆盖 |
| [ ] | 周/月 50% 门槛使用 `ceil(eligibleDays × 0.5)` | 纯单元 | 临界值上下界 |
| [ ] | 体重一条为有限、两条才计算趋势 | 纯单元/服务 | 0/1/2 条记录 |
| [ ] | 饮食无记录日不作零；热量达标固定为目标 90%–110% | 纯单元/服务 | 边界值断言 |
| [ ] | 宏量营养素单独展示平均、目标与差值 | 纯单元/Widget | 快照字段与页面 |
| [ ] | 饮水按有效记录日、总量、均值和目标达标统计 | 纯单元/服务 | 空/部分/充分数据 |
| [ ] | 运动有计划按次数一半，无计划至少两条记录 | 纯单元/服务 | 两种充分性路径 |
| [ ] | 睡眠夜间、小睡、已评价质量和目标分别统计 | 纯单元/服务 | 混合记录测试 |
| [ ] | 任务空分母为不适用；延期多次只计一次 | 纯单元/服务 | 统计边界测试 |
| [ ] | 不生成综合健康分、诊断或主观健康等级 | 纯单元/隐私审查 | 快照与文案扫描 |

## 对比与建议矩阵

| 状态 | 验收项 | 验证层级 | 证据 |
|---|---|---|---|
| [ ] | 目标只使用周期内实际生效且可执行的计划版本 | 服务/HTTP | 跨计划版本测试 |
| [ ] | 无计划、暂停、待重算和风险阻断不生成目标结论 | 纯单元/Widget | 四种计划状态 |
| [ ] | 上一周期对比仅在两侧对应栏目都充分时出现 | 纯单元 | 充分性组合测试 |
| [ ] | 数量差、比例百分点和持平语义由服务端确定 | 纯单元/Dart | 契约解析测试 |
| [ ] | 最多三条建议，使用 `REPORT_RULES_V1` 和稳定优先级/去重/排序 | 纯单元 | 确定性测试 |
| [ ] | 数据不足使用中性中文，不输出“异常”“不健康”等诊断措辞 | 纯单元/Widget/PDF | 文案扫描 |

## 版本、并发与幂等矩阵

| 状态 | 验收项 | 验证层级 | 证据 |
|---|---|---|---|
| [ ] | 首次生成版本 1，快照不可原地修改 | 服务/HTTP | 生成与数据库断言 |
| [ ] | 规范事实稳定排序，同输入哈希确定且不含昵称 | 纯单元/服务 | 顺序扰动和昵称变更 |
| [ ] | 输入未变复用最新版，输入变化递增版本 | 服务/API 36 | 补录前后生成 |
| [ ] | 旧快照和旧 PDF 在新版本生成后保持不变 | 服务/PDF/API 36 | 内容哈希对比 |
| [ ] | 同用户同幂等键返回相同结果，不同请求复用键为中文 409 | 服务/HTTP/Dart | 重试与冲突测试 |
| [ ] | 同周期并发生成只保存一个相同输入版本 | 服务/HTTP | 并发竞争测试 |
| [ ] | 生成失败不留下半快照或孤立来源 | 服务/故障注入 | 事务回滚测试 |
| [ ] | Flutter 防重复点击并丢弃账号/周期/页面变化后的迟到响应 | Dart/Widget | 控制器竞态测试 |

## 来源、删除、权限与隐私矩阵

| 状态 | 验收项 | 验证层级 | 证据 |
|---|---|---|---|
| [ ] | 六类统计均可追溯到类型、资源、日期和中文定位标签 | 服务/HTTP/Widget | 来源列表与回跳 |
| [ ] | 来源已删除时显示中文不可定位状态，不使报告崩溃 | 服务/Widget | 删除源后读取 |
| [ ] | 删除指定版本幂等，其他版本和全部源数据不变 | 服务/HTTP/API 36 | 前后数据库断言 |
| [ ] | 游客仅看固定示例且不请求私人接口 | HTTP/Widget/API 36 | 401 与网络调用断言 |
| [ ] | 已登录未建档显示中文引导，建档后回到原报告意图 | HTTP/Widget/API 36 | 权限闭环 |
| [ ] | 已建档无计划仍可生成，计划栏目为不适用 | 服务/Widget/API 36 | 无计划路径 |
| [ ] | 列表、详情、来源、PDF、删除均跨账号安全返回 | HTTP/API 36 | 双账号测试 |
| [ ] | 请求不接受用户 ID、统计、建议或版本等服务端权威字段 | HTTP/Dart | 请求体断言 |
| [ ] | 快照、PDF、响应和日志不含手机号、完整出生日期、登录信息、问卷原文、幂等键或输入哈希 | 服务/HTTP/PDF/扫描 | 隐私夹具与日志扫描 |
| [ ] | 退出、换账号、会话过期清理内存与缓存，不泄露上一账号数据 | Dart/Widget/API 36 | 账号切换测试 |

## PDF 矩阵

| 状态 | 验收项 | 验证层级 | 证据 |
|---|---|---|---|
| [ ] | PDF 只由指定不可变快照流式生成，不重新读实时数据 | 服务/PDF | 旧版本内容稳定测试 |
| [ ] | `%PDF` 文件头、非空页、嵌入中文字体和关键中文可验证 | PDF/API 36 | 解析与实际打开 |
| [ ] | 昵称、周期、状态、版本、结论、六栏目、建议和免责声明齐全 | PDF | 内容测试 |
| [ ] | 响应为 `application/pdf`，禁止共享缓存，中文安全文件名编码正确 | HTTP/Dart | 响应头测试 |
| [ ] | PDF 不写数据库、不留服务器文件、不生成公开 URL | 服务/仓库审查 | 存储与目录检查 |
| [ ] | PDF 失败为中文且不修改报告，客户端保留报告并可重试 | HTTP/Dart/Widget | 故障测试 |
| [ ] | 字体来自官方许可源，仓库只含实际字体及完整许可证 | 许可证/仓库审查 | 来源、哈希和许可文件 |
| [ ] | Android 下载后能实际打开，中文无方框且分页不截字 | Android API 36 | 下载文件与阅读器实测 |

## Flutter 缓存与可访问性矩阵

| 状态 | 验收项 | 验证层级 | 证据 |
|---|---|---|---|
| [ ] | 缓存按账号、类型、周期和版本隔离 | Dart/Widget | 键空间测试 |
| [ ] | 缓存只作离线只读展示，明确“数据可能不是最新” | Dart/Widget/API 36 | 断网测试 |
| [ ] | 生成失败保留旧报告与周期，删除后选择下一版本或空态 | Dart/Widget | 控制器状态测试 |
| [ ] | 日/周/月切换、结论优先布局、关键数字、关注项与六栏目正确 | Widget/API 36 | 页面测试 |
| [ ] | 历史版本、重新生成、删除确认、依据和 PDF 状态全部中文 | Widget/API 36 | 交互测试 |
| [ ] | 新增主要控件统一 6 px 圆角，不只靠颜色表达 | Widget/人工审查 | 样式断言 |
| [ ] | 360 px/720×1600 不横向溢出，键盘和安全区可滚动 | Widget/API 36 | 窄屏测试 |
| [ ] | 1.3x 和 2.0x 字体可用，关键控件触控区与读屏语义完整 | Widget/API 36 | 大字体与语义测试 |
| [ ] | 减少动画设置下无不必要动画，Android 返回与底部导航正确 | Widget/API 36 | 平台设置实测 |

## Android API 36 端到端矩阵

| 状态 | 场景 | 预期证据 |
|---|---|---|
| [ ] | H2 后端监听 `0.0.0.0:8080`，App 使用 `10.0.2.2:8080` | 健康检查、后端日志、设备网络 |
| [ ] | 游客示例、登录未建档拦截、建档回跳、完整用户 | 四条主路径触控 |
| [ ] | 跨日、跨周、跨月六类数据 | 数据准备记录与来源计数 |
| [ ] | 日报、当前周报、结束周报、月报 | 周期、进行中状态和六栏目 |
| [ ] | 补录后数据变化提示与新版本 | 新旧版本和快照哈希 |
| [ ] | 连点与网络重试幂等 | 版本列表无重复 |
| [ ] | 来源跳转和返回 | 返回后周期、版本保持 |
| [ ] | 删除旧版本 | 最新版本和源数据仍存在 |
| [ ] | 断网缓存与联网恢复 | 旧报告保留，重试成功 |
| [ ] | 中文 PDF 下载和实际打开 | 文件名、页面、字体、指标与隐私检查 |
| [ ] | 跨账号、退出、重登和会话过期 | 前账号报告与缓存不可见 |
| [ ] | 720×1600、1.3x、2.0x、键盘、语义、减少动画 | 无溢出且可操作；设置恢复 |
| [ ] | 环境恢复 | 8080 释放、模拟器关闭、工作树干净 |

## 最终交付记录

### 任务 15–16 结果

- 独立契约回归补充 `ReportDataCollectorTests` 2 项、`ReportServiceTests` 5 项，覆盖历史周期计划截断、昵称不参与哈希、`sourceDataChanged`、100 字符幂等键与复用冲突、跨服务实例并发唯一、事务回滚、来源用户隔离、重复删除和删除后版本递增。
- 经失败测试或设备实测证实并最小修复：历史详情错误标记 `created`；删除后复用版本号违反唯一约束；多服务实例并发可能重复版本；历史周期错误读取后来创建的计划；PDF 流式响应的异步分派被安全链再次拒绝。
- V11 迁移在空库和 V10→V11 的 H2 MySQL 模式通过；唯一键严格为 `(user_id, report_type, period_start, version)`，来源组合唯一键为 `(report_id, section_code, metric_code, source_type, source_id)`。
- 后端全量 160 项通过；Flutter 全量 186 项通过；`dart format` 103 文件、0 变化；`flutter analyze` 无问题；Debug APK 构建通过；`scripts/check.ps1` 通过。总检查首次复跑因主机原生内存不足导致 Surefire JVM 启动失败，释放 Gradle daemon 后迁移测试与总检查均通过，非断言或产品失败。
- Android API 36（`emulator-5554`）连接 H2 后端 `0.0.0.0:8080` / App `10.0.2.2:8080`：触控验证游客中文示例与登录限制、未建档引导、七步建档、无计划、日/周/月生成、空/部分数据、补录饮水后的数据变化提示、历史版本 1/2、中文免责声明和 PDF 下载。
- PDF 设备下载文件 `健康报告-2026-09-07-第1版.pdf`，31,876 字节；应用无现成打开能力，按计划回退为保存并明确文件位置。自动化同时验证 `%PDF`、非空页、中文字体嵌入、关键中文、免责声明及手机号/完整生日/登录信息/风险问卷缺失。
- 720×1600 下分别以 1.3x、2.0x 字体完成报告页面触控、滚动与底部导航检查；所有节点保持屏幕边界内，恢复为默认 1080×2400、1.0x。
- 缓存隔离、断网保留旧报告、账号切换和迟到响应由 Dart/Widget 自动化覆盖；设备完成在线缓存写入。冷启动断网会进入既有登录闭环，因此未把“冷启动仍展示私人缓存”作为通过项。
- PDFBox `3.0.8` 为 Apache-2.0；LXGW WenKai 字体随附完整 SIL OFL 1.1 许可证。仓库与敏感文件检查通过，APK、设备 PDF、缓存和构建输出均未纳入提交。
- 本机只有 MySQL 5.7.44，且 Docker 不可用；MySQL 8.4 的 V1→V11、V10→V11、并发 PDF、备份恢复仍是发布前阻断门禁，不得标记完成。
- 结论：除明确的 MySQL 8.4 发布门禁与设备无现成 PDF 阅读器集成外，候选满足合并到 `main` 的代码门禁；本任务不推送 `main`。
