# 轻食记「今日执行中心」QA 验收矩阵

日期：2026-09-08

验收分支：`codex/today-final-qa`

设计与计划基线：`origin/main@4f353b3195b36ed9628829644e266797dd5bca13`

当前阶段：候选已集成，自动化验收通过；Android 设备实测待测

## 1. 范围与判定原则

本矩阵覆盖 `GET /api/v1/today?date=YYYY-MM-DD`、Flutter 今日页、账户权限闭环、模块降级、中文化、轮播可访问性、减少动画、跨日刷新和交付检查。

验收只认可真实计划和身体测量数据。营养、饮水、运动、睡眠和任务在首版没有记录数据时必须显示 `COMING_SOON`，不得展示健康评分、连续打卡、完成率、趋势或推导建议。单模块失败不得升级为整个聚合接口失败。

状态说明：

- `通过`：已在当前提交执行并留有证据。
- `待集成`：基线尚无今日执行中心实现，需在候选代码合入 QA 分支后验证。
- `设备待测`：自动化或构建可验证，但当前无 Android 模拟器/真机。

## 2. 基线与候选登记

| 项目 | 结果 |
|---|---|
| 工作树 | `E:\Healthy` |
| QA 分支 | `codex/today-final-qa`，从 `origin/main` 创建 |
| 基线提交 | `4f353b3195b36ed9628829644e266797dd5bca13` |
| QA 文档候选 | `779c72779c0ca20b40630f8c315d3423b289f49e`；已集成为 `8ce59a9eb593de46393b236088021cc8d1d7c549` |
| 后端候选 | `3fc59c5fc6e9b32ff616be48187743e03504e3f8`（包含 `c0618db`、`b71940b`）；已合并为 `2df997c39147c503df3c075c4c9671974756ed96` |
| Flutter 候选 | `1f649099971f2ab66943f3d9b59f40241eb73215`；已合并为 `c7b18b0f530ba162dc53ad4353185a93fe1e8b70` |
| QA 修复 | `e438790456ae2817b6653ebb07497a1e2ea7faec`：换号缓存隔离与 6px 弹层圆角；`8c45e84ad3375a32b9bd9ba1ed813cb2e3d34682`：销毁后丢弃在途响应与本地跨日回归 |
| main 操作 | 未修改、未推送 |

## 3. 后端接口验收矩阵

| ID | 场景与操作 | 预期结果 | 状态/证据 |
|---|---|---|---|
| API-01 | 未携带 Token 请求合法日期 | 401，统一错误结构，不返回私人字段 | 待集成 |
| API-02 | 携带失效或伪造 Token | 401，不进入聚合服务 | 待集成 |
| API-03 | `GET /today?date=2026-09-08` | 200；响应 `date` 精确等于请求本地日期 | 待集成 |
| API-04 | 缺少 `date` | 4xx 统一中文校验错误；不得使用服务器日期静默代替 | 待集成 |
| API-05 | `date=2026/09/08`、时间戳、非法日/月、空串 | 400；稳定错误码和中文消息；不得返回 500 | 待集成；重点检查 Spring 参数绑定异常处理 |
| API-06 | 客户端尝试附带 `userId` | 服务只使用认证主体；参数不能改变数据归属 | 待集成 |
| API-07 | 用户 A、B 均有计划和体重 | A 的响应不含 B 的目标、体重、计划 ID 或内部标识 | 待集成 |
| API-08 | 无健康档案 | `plan=EMPTY`；下一步为 `COMPLETE_PROFILE/完善健康档案` | 待集成 |
| API-09 | 档案未完成或缺代谢依据 | 同 API-08，不得被 `completed` 历史值误判 | 待集成 |
| API-10 | 档案完整、无计划 | `plan=EMPTY`；最新体重可独立 `READY`；下一步 `CREATE_PLAN` | 待集成 |
| API-11 | 活跃计划 | `plan=READY`；当前周和所有目标字段来自当前计划版本 | 待集成 |
| API-12 | 计划需重算 | 状态清晰映射 `NEEDS_RECALCULATION`；不得当作普通生效目标引导 | 待集成 |
| API-13 | 计划暂停 | 返回暂停状态和确定性 `RESUME_PLAN/查看暂停的计划` | 待集成 |
| API-14 | 风险拦截 | 风险优先；不返回或不展示普通减脂目标；`VIEW_RISK_GUIDANCE` | 待集成 |
| API-15 | 服务端不存在持久化待确认计划 | 后端不得猜测 `CONFIRM_PLAN`；该覆盖只由仍持有预览的客户端完成 | 待集成 |
| API-16 | 当前计划字段映射 | `targetKcal/proteinG/carbsG/fatG/waterMl/exerciseDays/exerciseMinutes/sleepHours` 精确匹配 `PlanService` 结果 | 待集成 |
| API-17 | 当前周计算 | 边界日、暂停日及第 1/4 周映射正确；明确请求日期与服务端“当前”语义 | 待集成；重点审查是否错误使用 `LocalDate.now()` |
| API-18 | 同用户多条身体测量 | 选 `measured_at` 最新记录；时间相同使用稳定次序 | 待集成 |
| API-19 | 请求日期早于/晚于最新测量 | 首版仍返回“最近一条”的既定口径，不虚构当天记录 | 待集成 |
| API-20 | 无身体测量 | `weight=EMPTY`，计划模块仍正常 | 待集成 |
| API-21 | 计划读取抛异常 | 200；仅 `plan=ERROR`，体重与占位模块仍返回 | 待集成 |
| API-22 | 体重读取抛异常 | 200；仅 `weight=ERROR`，计划与占位模块仍返回 | 待集成 |
| API-23 | 计划和体重均异常 | 200；二者独立 `ERROR`，响应结构仍完整 | 待集成 |
| API-24 | 首版占位模块 | `nutrition/hydration/activity/sleep/tasks` 固定 `COMING_SOON` | 待集成 |
| API-25 | 响应数据最小化 | 不含 `userId`、Token、异常、SQL、计划历史或数据库字段 | 待集成 |
| API-26 | 模块状态向前兼容 | 仅使用 `READY/EMPTY/COMING_SOON/ERROR`，各模块保留独立结构 | 待集成 |
| API-27 | 并发请求和跨账户请求 | 无共享可变状态、无缓存串号；每次按认证主体加载 | 待集成 |
| API-28 | 实现边界 | 不新增迁移、表、队列、缓存框架、公式副本或依赖 | 待集成；代码审查 |

## 4. Flutter 数据、状态与页面验收矩阵

| ID | 场景与操作 | 预期结果 | 状态/证据 |
|---|---|---|---|
| APP-01 | 游客打开今日页 | 不请求 `/today`，只显示不含虚构数值的能力预览 | 待集成 |
| APP-02 | 游客点击受限行动 | 进入现有登录闭环，完成后保留原目标功能 | 待集成 |
| APP-03 | 已登录未建档 | 请求合法时显示“完善健康档案”，行动进入既有建档流程 | 待集成 |
| APP-04 | 已建档无计划 | 展示最新真实体重和“生成减脂计划”，进入计划页 | 待集成 |
| APP-05 | 客户端仍持有计划预览 | 下一步覆盖为“继续确认计划”；目标数值不得取自内存预览 | 待集成 |
| APP-06 | 计划生效 | 展示真实热量、营养、饮水、运动、睡眠、体重和当前周 | 待集成 |
| APP-07 | 计划暂停 | 明确显示暂停；行动进入计划页恢复/调整 | 待集成 |
| APP-08 | 风险拦截 | 不显示普通减脂目标；显示咨询医生或注册营养师建议 | 待集成 |
| APP-09 | 计划需重算 | 明确提示重算并进入现有计划流程，不伪装成正常生效 | 待集成 |
| APP-10 | 首次加载 | 显示骨架或明确加载态，不闪现旧账户数据 | 待集成 |
| APP-11 | 首次加载成功 | 保存当前账户、当前日期的最近成功响应 | 待集成 |
| APP-12 | 首次加载失败且无缓存 | 中文整页错误态和“重新加载” | 待集成 |
| APP-13 | 下拉刷新成功 | 使用同一设备本地日期重新请求并更新内容 | 待集成 |
| APP-14 | 刷新失败且有旧数据 | 保留旧内容，非阻断提示“刷新失败，当前内容可能不是最新” | 待集成 |
| APP-15 | 重复下拉或生命周期并发刷新 | 合并或拒绝重复请求，不出现旧响应覆盖新响应 | 待集成 |
| APP-16 | 登出后以游客浏览 | 清除/隔离私人成功缓存，不显示前一账户目标和体重 | 待集成；高风险权限项 |
| APP-17 | A 登出后 B 登录 | 缓存按账户隔离或控制器重建，B 不看到 A 数据 | 待集成；高风险权限项 |
| APP-18 | App 从后台恢复但仍是同一天 | 不做无意义重复刷新 | 待集成 |
| APP-19 | App 后台跨本地午夜后恢复 | 切换为设备新本地日期，清晰加载新日数据 | 待集成 |
| APP-20 | 时区变化、夏令时或系统时间回拨 | 比较本地 `DateTime` 的年月日，不按 UTC 日期或固定 24 小时推算 | 待集成 |
| APP-21 | 跨日请求在途时再次刷新 | 旧日期响应不得覆盖新日期页面 | 待集成 |
| APP-22 | `plan=ERROR` 或 `weight=ERROR` | 对应卡片显示“该项数据暂时无法加载”，其他卡片正常 | 待集成 |
| APP-23 | `COMING_SOON` | 显示“记录功能待接入”，不得进入空白页 | 待集成 |
| APP-24 | 未知模块状态或新 JSON 字段 | 模型安全降级，页面不崩溃；忽略未知字段 | 待集成 |
| APP-25 | 缺少可选字段 | 模型可解析；不以 `null`、英文枚举或 `0` 伪装真实数据 | 待集成 |
| APP-26 | 网络不可用 | `网络连接失败，请稍后重试` | 待集成 |
| APP-27 | 后端 5xx | `服务暂时不可用，请稍后重试`，不显示堆栈或内部消息 | 待集成 |
| APP-28 | 所有用户可见状态 | 标题、错误、按钮、语义标签均为中文；不裸露枚举名 | 待集成 |
| APP-29 | 点击核心目标或 `VIEW_PLAN` | 切换到底部“计划”，复用现有导航状态 | 待集成 |
| APP-30 | 快捷记录饮食/饮水/运动/睡眠 | 未上线时统一显示中文说明，不误触其他模块 | 待集成 |
| APP-31 | 320px 窄屏 | 无横向溢出、截断导致的关键行动不可见 | 待集成 |
| APP-32 | 200% 大字体 | 可滚动、无溢出，重要数值与操作仍可访问 | 待集成 |
| APP-33 | 读屏 | 轮播页、图片含义、核心目标、状态和按钮有明确语义 | 待集成 |
| APP-34 | 所有主要卡片、按钮、轮播和弹层 | 主圆角为 6px，不出现大面积胶囊控件 | 待集成 |

## 5. 轮播专项矩阵与资源检查

三张候选源图均为 PNG、1536×672（比例约 2.29:1），可离线打包；逐张人工查看均为写实健康餐饮横幅，清晰度、主体留白和横向裁切基础条件通过。

| 资源 | 文件大小 | 画面 | 基线结论 |
|---|---:|---|---|
| `assets/figma-recipe-1.png` | 1,542,451 B | 藜麦、豆腐、牛油果与蔬菜沙拉 | 通过 |
| `assets/figma-recipe-2.png` | 1,396,112 B | 牛肉、杂粮面与蔬菜 | 通过；实现后检查文案不要暗示未经证实的营养结论 |
| `assets/figma-recipe-3.png` | 1,156,726 B | 酸奶、谷物/南瓜与籽类杯 | 通过 |

| ID | 场景与操作 | 预期结果 | 状态/证据 |
|---|---|---|---|
| CAR-01 | 离线启动 | 三张本地图片仍显示，无网络占位或远程 URL | 待集成 |
| CAR-02 | 自动轮播 | 正常动画偏好下按确定间隔切换，页码指示同步 | 待集成 |
| CAR-03 | 手动左右滑动 | 手势始终可用，自动计时从用户操作后重新计算 | 待集成 |
| CAR-04 | 系统开启减少动画 | 自动播放停止，手动滑动仍可用 | 待集成；必须覆盖 `MediaQuery.disableAnimations` |
| CAR-05 | App 后台或控件销毁 | 定时器和 `PageController` 正确停止/释放，无销毁后回调 | 待集成 |
| CAR-06 | 深浅画面叠加中文 | 遮罩保证足够对比；不依赖图片本身亮度 | 待集成 |
| CAR-07 | 窄屏与大字体 | `BoxFit.cover` 裁切不丢主要食物，文案和指示器不溢出 | 待集成 |
| CAR-08 | 读屏浏览 | 每张图有独立中文语义，页码可理解，不重复朗读装饰图 | 待集成 |

## 6. 基线执行记录

| 检查 | 命令 | 结果 |
|---|---|---|
| 后端全量测试 | `backend\mvnw.cmd test` | 通过，25 项，0 失败/错误/跳过 |
| Flutter 全量测试 | `flutter test` | 通过，28 项 |
| Flutter 静态分析 | `flutter analyze` | 通过，0 issues |
| Android Debug APK | `flutter build apk --debug` | 通过，`mobile/build/app/outputs/flutter-apk/app-debug.apk` |
| 仓库检查 | `scripts\check.ps1` | 通过；内部再次执行后端 25 项 |
| 空白差异 | `git diff --check` | 通过 |
| 敏感文件与高置信凭据模式 | Git 文件名与内容扫描（排除 `.env.example`） | 通过 |
| Android 设备实测 | `flutter devices` | 设备待测；仅发现 Windows、Chrome、Edge，无 Android 设备 |

非阻断环境提示：Flutter 报告 10 个受当前约束限制的较新依赖版本；Flyway 提示 H2 2.4.240 高于其已验证的 2.3.232；Gradle/JDK 报未来版本将限制未声明 native access。以上均未导致当前测试或构建失败。

## 7. 集成阻断风险清单

| 风险 | 严重度 | 集成放行条件 |
|---|---|---|
| 非法日期绑定落入通用异常处理并返回 500 | 高 | 集成测试证明所有非法日期为 400、统一错误码和中文消息 |
| 聚合服务使用服务器 `LocalDate.now()` 计算请求日期的周数 | 高 | 明确契约并覆盖请求日期与服务器日期不同的测试 |
| 今日成功缓存未按账户隔离，登出/换号泄露数据 | 高 | 登出和 A→B 换号 Widget/控制器测试通过 |
| 风险拦截、暂停、需重算状态优先级错误 | 高 | 状态组合测试证明风险不泄露普通目标、行动确定性正确 |
| 单模块异常被过宽/过窄捕获，导致全接口失败或掩盖编程错误 | 高 | 计划/体重分别及同时失败的服务测试与 200 响应通过 |
| 生命周期只在页面重建时取日期，后台跨日不刷新 | 高 | 可注入时钟或明确日期测试覆盖同日与跨日本地午夜 |
| 旧日期在途响应覆盖新日期 | 中 | 控制器并发与跨日竞态测试通过 |
| 减少动画只降低时长但 Timer 仍自动翻页 | 中 | 开启减少动画后的计时器行为测试通过 |
| `IndexedStack` 中不可见今日页仍持续轮播/刷新 | 中 | 后台与销毁行为无回调异常；必要时停止不可见动画 |
| API 未知枚举、缺字段或 5xx 被显示为英文/内部信息 | 中 | 模型降级和中文错误映射测试通过 |
| 图片虽高清但叠字在浅色区域对比不足 | 中 | 深色渐变遮罩、大字体及语义测试通过 |

## 8. 最终集成放行门槛

1. 后端、Flutter 候选均以明确提交哈希合入 `codex/today-final-qa`，不得直接修改或推送 `main`。
2. API-01 至 API-28、APP-01 至 APP-34、CAR-01 至 CAR-08 均有自动化或人工证据；高风险项不得未决。
3. 后端全量测试、Flutter 全量测试、`flutter analyze`、Debug APK、`scripts/check.ps1`、`git diff --check` 和敏感信息扫描全部通过。
4. 无 Android 设备时必须保留“设备待测”，不得把 APK 构建成功描述为真机验收。
5. 最终报告列出合并提交、QA 修复提交、通过项、失败/未测项及是否建议合入 `main`。

## 9. 最终集成与验收记录

本节为最终结果，优先于上文候选集成前的“待集成”标记。三项候选从精确基线 `origin/main@4f353b3195b36ed9628829644e266797dd5bca13` 集成，无文本冲突；未推送任何分支，未合并或修改 `main`。

### 9.1 QA 修复

代码复核发现 `todayControllerProvider` 以长期存活的 `ApiClient` 为键，登出后换号可能保留前一账户同日成功数据；若新账户请求失败，存在把旧数据作为 stale 内容展示的风险。修复将提供器改为 `autoDispose`，页面离开后立即释放用户数据，并增加先失败后通过的 provider 生命周期及账户切换回归测试。控制器销毁后会丢弃仍在途的成功或失败响应；缓存按请求日期切换，跨日不复用旧日期数据，并通过可注入本地时钟验证同日恢复不重复请求、跨日恢复重新加载。

同时为全局 `DialogTheme` 补充 6px 圆角和测试，使风险提示等主要弹层符合视觉规格。未新增依赖、持久缓存、数据库表或业务抽象。

### 9.2 重点场景结果

| 场景 | 结果与证据 |
|---|---|
| 缺少或非法 `date` | 通过；`TodayIntegrationTests.requiresAuthenticationAndValidDate` 验证缺参数与错误格式均为 400、统一中文错误 |
| 档案不完整 | 通过；历史计划目标被隐藏，返回 `PROFILE_INCOMPLETE` 与 `COMPLETE_PROFILE`；Flutter 未建档不请求私人接口 |
| 风险拦截 | 通过；不返回/展示普通目标，行动为 `VIEW_RISK_GUIDANCE`，中文健康建议 |
| 暂停与需重算 | 通过；后端状态及 Flutter 中文行动均有自动化覆盖 |
| 模块独立失败 | 通过；计划、体重分别失败及同时失败均保持聚合结构，错误不泄露内部异常 |
| 游客访问 | 通过；Widget 测试证明游客不调用 `/today` 且不显示虚构数值 |
| 用户与日期隔离 | 通过；后端 A/B 数据隔离、Flutter 跨日串行更新、页面离开释放账户缓存均有自动化覆盖 |
| 全中文 | 通过；错误、状态、行动、快捷入口和语义标签均为中文；计量单位保留标准缩写 |
| 6px 圆角 | 通过；卡片、按钮、轮播、底部弹层及对话框统一，新增主题回归测试 |
| 真实本地轮播图片 | 通过；3 张 1536×672 PNG 均随包本地加载并人工复核为写实健康餐食图片 |
| 减少动画 | 通过；`MediaQuery.disableAnimations=true` 时 6 秒后仍停在首图，手动滑动保留，卸载无计时器异常 |
| 大字体与窄屏 | 通过；320px、200% 字体 Widget 测试无溢出，核心区域可滚动访问 |

### 9.3 最终命令结果

| 检查 | 最终结果 |
|---|---|
| `backend\\mvnw.cmd test` | 通过，36 项，0 失败/错误/跳过 |
| `dart format --output=none --set-exit-if-changed lib test` | 通过，36 个文件，0 个需修改 |
| `flutter analyze` | 通过，0 issues |
| `flutter test` | 通过，55 项 |
| `flutter build apk --debug` | 通过；`mobile/build/app/outputs/flutter-apk/app-debug.apk`，206,966,311 B，SHA-256 `97445a5240d41b01e0199c9e6ae6977bb76a1208fafcc5e2ffbc0935649a6cbc` |
| `scripts\\check.ps1` | 通过；内部后端 36 项全部通过，输出 `All project checks passed.` |
| `git diff --check` | 通过 |
| 敏感文件名、高置信凭据模式与跟踪构建产物扫描 | 通过；敏感文件名 0、高置信内容命中 0、跟踪构建产物 0；本地 `.env` 由 `.gitignore` 排除 |
| `flutter devices` | 未发现 Android 模拟器/真机；仅 Windows、Chrome、Edge |

非阻断环境提示：Flutter 报告 10 个受当前约束限制的较新依赖版本；Flyway 提示 H2 2.4.240 高于其已验证的 2.3.232；Gradle/JDK 报未来版本将限制未声明 native access。以上均未导致测试、分析或构建失败。

### 9.4 放行结论

自动化、静态检查、APK 构建、代码复核和资源人工检查全部通过，建议进入 Android 设备实测。由于当前没有 Android 模拟器或真机，游客、登录、建档、计划状态切换、刷新与触控轮播的设备实测明确标记为 **设备待测**；在设备实测完成前不表述为真机验收通过。

### 9.5 相对 `codex/today-qa@21dc288365dc722958f49c2f9142053e86edbfea` 的独立复核差异

当前候选已纳入该提交的全部有效运行时修复：`autoDispose` 会话隔离、控制器销毁后的在途响应保护，以及可注入本地时钟。对比后的额外有效差异仅为：

- 全局 `DialogTheme` 使用 6px 圆角，补齐风险提示等主要弹层的设计规格。
- 增加 provider 自身“最后一个观察者离开即释放缓存”的生命周期测试，与账户切换 Widget 测试形成两层证据。
- 增加 6px 对话框主题回归测试。

除上述 3 项外，当前 `mobile` 运行时代码与 `21dc288365dc722958f49c2f9142053e86edbfea` 一致；差异统计为 1 个运行时主题文件新增 3 行、2 个测试文件新增 33 行。最终 Flutter 全量测试因此由对方的 53 项增加为 55 项。
