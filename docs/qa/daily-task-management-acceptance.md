# 每日任务管理独立验收记录

日期：2026-09-10

QA 分支：`codex/tasks-qa`

独立工作树：`E:\Healthy-worktrees\tasks-qa`

## 共同基线

- 精确基线：`8c923ae0181c5018bc0d3bbdf8adca52accf7d5a`
- 后端候选：`db4f63fd872f7235d39c4e3f8987841cc7eae2a8`
- Flutter 候选：`2ce20f3b7fe26b395ad77772d45e055dd1115a3c`
- QA 最终候选：见“最终交付记录”（提交后回填）
- Git 邮箱：`3040503900@qq.com`

本验收记录先于产品候选建立。以下矩阵中的“待候选/待设备”保留任务 1 建立基线时的状态快照；任务 15–16 的最终覆盖和结论统一记录在文末，后者为最终状态。

## 环境与未合并候选基线

| 项目 | 环境或命令 | 结果 |
|---|---|---|
| Java | OpenJDK `17.0.19` | 可用 |
| Flutter / Dart | Flutter `3.47.2`、Dart `3.13.2` | 可用 |
| Android SDK | `C:\Users\Li\AppData\Local\Android\Sdk`；已安装 `android-35`、`android-36`、`android-36-2`、`android-37.0` | 可用 |
| ADB | `1.0.41`（platform-tools `37.0.1-15733141`） | 可用 |
| 模拟器 | Android Emulator `37.1.11.0`；AVD `LightBite_API_36` | 已安装，基线时未在线；不阻断并行开发 |
| 后端全量 | `backend\.\mvnw.cmd test` | 通过：112 项，0 失败、0 错误、0 跳过 |
| Dart 格式 | `dart format --output=none --set-exit-if-changed lib test` | 通过，无文件变化 |
| Flutter 静态分析 | `flutter analyze` | 通过：`No issues found` |
| Flutter 全量 | `flutter test` | 通过：137 项 |
| 仓库总检查 | `.\scripts\check.ps1` | 通过：`All project checks passed`，其中后端 112 项再次通过 |
| 空白与状态 | `git diff --check`、`git status --short --branch` | 通过；提交前仅本验收文档有变化 |

非阻断基线提示：H2 `2.4.240` 高于 Flyway 已验证版本 `2.3.232`；11 个 Flutter 包有与当前约束不兼容的新版本。

状态说明：`待候选` 表示需在后端/Flutter 候选合并后验收；`待设备` 表示需最终 APK 与 API 36 触控证据；`通过` 必须记录自动化或设备证据；`阻断` 表示不可形成最终候选。

## 数据库验收矩阵

| 验收项 | 证据 | 状态 |
|---|---|---|
| 空库直接迁移至 V10 | Flyway 空库测试；四表、外键、索引、唯一约束和检查约束审查 | 待候选 |
| V8→V9→V10 | 迁移前后账号、档案、计划、饮食、饮水、运动、睡眠行值断言 | 待候选 |
| V9→V10 | 已有模板与实例完整保留，设置和通知事件表正确新增 | 待候选 |
| MySQL 8.4 兼容 | 有可用实例时执行真实迁移；不可用时明确记录环境限制 | 待候选 |
| 实例唯一性 | `(template_id, original_local_date)` 在串行和并发生成下均不重复 | 待候选 |
| 计划模板唯一性 | 用户、计划版本和稳定任务代码防止重复模板 | 待候选 |
| 勿扰默认值 | 每账号一行，默认开启且为 `22:30–07:00` | 待候选 |
| 通知事件约束 | 仅 `SCHEDULED/CANCELLED/OPENED`；用户＋幂等键唯一；不保存原始设备标识 | 待候选 |
| 数据边界 | 来源、分类、优先级、重复、状态、提醒、全天/时间、标题和备注约束完整 | 待候选 |

## 服务验收矩阵

| 验收项 | 证据 | 状态 |
|---|---|---|
| 五种重复规则 | `NONE/DAILY/WEEKDAYS/WEEKENDS/WEEKLY_DAYS` 日期命中和非法位图 | 待候选 |
| 生成范围 | 今天至未来 7 天稳定生成；重复读取幂等；历史缺失实例不回填；超范围拒绝 | 待候选 |
| 时区与 DST | IANA/固定偏移、跨日、周边界；不存在/重复本地时间返回中文字段错误 | 待候选 |
| 用户任务边界 | 标题 1–80、备注 500、分类、优先级、全天/时间、提醒和重复校验 | 待候选 |
| 状态闭环 | 完成/撤销、跳过/撤销、动态逾期，状态字段清理和完成来源正确 | 待候选 |
| 延期 | 稍后 30 分钟、明天、自选时间；跨日归档；保留原日期/截止；多次延期统计一次 | 待候选 |
| 编辑范围 | 仅今天只写实例覆盖；之后安排只更新未执行、未延期实例；版本冲突为中文 409 | 待候选 |
| 删除范围 | 仅删除今天；删除今天及以后；历史执行记录保留；计划禁用覆盖防止重建 | 待候选 |
| 幂等与并发 | 创建和状态操作串行/并发重试稳定，不同用户同键互不影响 | 待候选 |
| 健康计划生成 | 三餐、饮水、运动确定性分布和睡眠模板准确；暂停/恢复/更新及长期覆盖正确 | 待候选 |
| 四类健康联动 | 饮食、饮水、运动、睡眠满足条件自动完成；数据删除或降阈值后自动状态回滚 | 待候选 |
| 显式状态优先 | 用户手动完成、跳过或延期不被健康联动覆盖；手动完成不随健康数据删除撤销 | 待候选 |
| 7 天统计 | 应出现、完成、跳过、延期、逾期、分类明细和完成来源准确；分母为空返回空 | 待候选 |
| 通知与勿扰 | 提前 0/5/15/30/60 分钟；未来最多 7 天；跨午夜勿扰直接跳过且不补发 | 待候选 |
| Today 降级 | `READY/EMPTY/ERROR`、下一任务排序；任务失败不影响其他 Today 模块 | 待候选 |

## HTTP 验收矩阵

| 验收项 | 证据 | 状态 |
|---|---|---|
| 鉴权与隔离 | 全部 `/api/v1/tasks` 接口未登录为 401；身份仅来自认证主体 | 待候选 |
| 每日与周读取 | 日期/时区参数、时间线/全天/折叠汇总、7 天边界和稳定返回结构 | 待候选 |
| 创建与状态 API | 必须携带 `Idempotency-Key`；同键重试返回同一结果；字段错误为中文统一结构 | 待候选 |
| 编辑与删除 API | 实例/模板范围语义、`effectiveDate`、版本冲突和安全的不存在响应 | 待候选 |
| 设置 API | 默认设置、合法更新、跨午夜、非法时间和乐观锁冲突 | 待候选 |
| 通知查询 | 只返回未来 7 天内有时间、开启提醒且待完成的实例 | 待候选 |
| 通知事件 | 批量上报三类可验证事件、幂等重试、跨用户和原始设备标识拒绝 | 待候选 |
| 计划更新采用 | 只有用户明确采用时切换到新计划默认安排 | 待候选 |
| 数据最小化 | 请求不得接受 `userId`、完成率或健康条件结果；响应不泄露 SQL、异常和设备原标识 | 待候选 |

## Dart 单元验收矩阵

| 验收项 | 证据 | 状态 |
|---|---|---|
| 模型解析 | 模板、实例、每日、7 天、设置和通知完整/可选字段；未知字段兼容 | 待候选 |
| 未知枚举 | 未知分类显示“其他”，未知状态不得误判为已完成 | 待候选 |
| API 契约 | 每日、周、通知、创建、编辑、状态、删除、设置、事件和采用计划默认路径/载荷 | 待候选 |
| 本地日期与时区 | 日期固定 `YYYY-MM-DD`，请求使用设备 IANA 时区 | 待候选 |
| 幂等草稿 | 创建/状态失败保留同一幂等键，成功后清除；表单失败保留输入 | 待候选 |
| 缓存隔离 | 最近成功缓存按账号＋日期隔离，退出/换号不显示旧数据 | 待候选 |
| 迟到响应 | 页面销毁、账号/日期/筛选切换后丢弃旧响应 | 待候选 |
| 乐观更新 | 完成、跳过、撤销和延期失败准确回滚；同实例防重复操作 | 待候选 |
| 通知纯策略 | 提前量、7 天过滤、全天排除、跨午夜勿扰和稳定独立 ID | 待候选 |
| 通知生命周期 | 编辑/延期重排，完成/跳过/删除取消，启动/恢复/时区/账号变化重排 | 待候选 |
| 模块隔离 | 任务取消不影响饮水或睡眠提醒；退出只取消当前账号任务通知 | 待候选 |

## Widget 验收矩阵

| 验收项 | 证据 | 状态 |
|---|---|---|
| 三种用户状态 | 游客示例与登录恢复；未建档可建个人任务；有档案/计划显示对应健康任务 | 待候选 |
| 页面结构 | 日期导航、完成进度、下一任务、时间线、今日待办、五类筛选和折叠区 | 待候选 |
| 状态操作 | 完成/撤销、跳过/撤销、三种延期、更多菜单和自动完成依据 | 待候选 |
| 创建与编辑 | 标题、备注、五类、三级优先级、全天/时间、五种重复和提醒校验 | 待候选 |
| 范围确认 | “仅修改今天/之后都按此安排”和“仅删除今天/删除今天及以后”中文弹窗 | 待候选 |
| 周概览与设置 | 真实数量、空完成率、分类进度；默认/跨午夜勿扰及权限设置入口 | 待候选 |
| Today 接入 | 真实摘要、下一任务入口，不新增底部导航；任务错误不影响其他卡片 | 待候选 |
| 错误与缓存 | 保存失败保留表单；缓存标记“数据可能不是最新”；失败操作可重试 | 待候选 |
| 中文与 6 px | 页面、按钮、状态、通知、确认和错误均中文；主要新增控件圆角 6 px | 待候选 |
| 可访问性 | 读屏语义、足够触控区、720×1600、1.3 倍字体、键盘、安全区和减少动画 | 待候选 |

## Android API 36 触控矩阵

| 场景 | 预期 | 状态 |
|---|---|---|
| 游客与登录恢复 | 游客可浏览示例，受限操作登录后恢复 | 待设备 |
| 未建档与健康入口 | 可创建工作/生活任务；健康任务仅提示补档 | 待设备 |
| 健康计划任务 | 完成档案和活动计划后出现三餐、饮水、运动、睡眠任务 | 待设备 |
| 重复规则 | 一次性、每天、工作日、周末、每周指定星期均可创建并正确出现 | 待设备 |
| 排列和筛选 | 有时间进入时间线、全天进入今日待办；五类筛选和三级优先级可辨识 | 待设备 |
| 状态与逾期 | 完成/撤销、跳过/撤销、稍后、明天、自选时间、逾期和跨天展示正确 | 待设备 |
| 编辑与删除范围 | 两种编辑范围、两种删除范围均有中文确认且结果正确 | 待设备 |
| 四类健康联动 | 新增/删除饮食、饮水、运动、睡眠记录时自动完成与回滚正确 | 待设备 |
| 用户优先级 | 手动完成、跳过或延期后健康联动不覆盖 | 待设备 |
| 7 天概览 | 数量、分类、延期一次计数准确；空分母不显示虚假百分比 | 待设备 |
| 通知与勿扰 | 提前量、跨午夜勿扰、编辑重排、完成取消和点击跳转正确 | 待设备 |
| 权限与模块隔离 | 允许/拒绝均不影响任务；当前账号取消不影响饮水/睡眠提醒 | 待设备 |
| 断网与回滚 | 保留最近成功缓存，失败乐观操作回滚；恢复后端后同键重试不重复 | 待设备 |
| 紧凑与无障碍 | 720×1600、1.3 倍字体、键盘、读屏、安全区和减少动画可用 | 待设备 |
| 环境恢复 | 恢复分辨率、字体、动画和网络，关闭模拟器并释放 8080 | 待设备 |

## 最终交付记录

### 集成与独立回归

- 在 `codex/tasks-qa` 依次合并后端候选 `db4f63fd872f7235d39c4e3f8987841cc7eae2a8` 和 Flutter 候选 `2ce20f3b7fe26b395ad77772d45e055dd1115a3c`。
- 独立契约核验确认并修复：实例版本误作模板版本、实例响应缺少重复元数据、完成/撤销时区放错请求位置、自选延期的 Dart/HTTP 载荷不匹配、设置写入使用 H2 专用 SQL。
- API 36 真后端触控进一步发现并修复：全天任务自选延期违反实例时间约束；现在该次实例转换为有时间实例，同时保留原计划日期/原截止审计语义。
- 新增回归覆盖模板与实例版本分离、重复元数据、自选本地日期/时间与时区、全天实例自选延期；其余矩阵由候选既有数据库、服务、HTTP、Dart 和 Widget 测试覆盖。

### 最终自动化结果

| 检查 | 最终结果 |
|---|---|
| 后端全量 | 141 项通过，0 失败、0 错误、0 跳过 |
| Flutter 全量 | 168 项通过 |
| Flutter analyze | `No issues found` |
| Dart format | 94 个文件，0 个变化 |
| Debug APK | 构建通过 |
| 仓库检查 | `scripts/check.ps1` 通过；`git diff --check` 通过 |
| 仓库范围/敏感路径 | 相对共同基线 42 个受控文件；未包含 `.env`、密钥库、私钥或凭据文件 |

真实 MySQL 8.4 实例不可用，因此未执行 MySQL 8.4 实库迁移；本机仅有 MySQL 5.7，不冒充目标环境。迁移已通过空库、V8→V9、V9→V10、V8→V10 和 H2 MySQL 模式回归。H2/Flyway 版本提示与 11 个受约束包存在更新仍为非阻断环境提示。

### Android API 36 触控结果

- 设备：`LightBite_API_36`，Android API 36，1080×2400，字体比例 1.0。
- H2 真后端链路：验证码登录、未建档 Today 降级、进入每日任务、新建全天重复任务、分类/优先级、完成/撤销、自选延期并跨日进入时间线、重复任务“之后都按此安排”、最近 7 天概览、跨午夜勿扰设置均通过。
- 全天任务自选延期修复后复测：次日时间线显示 `QA_FINAL` 和本地时刻，服务端无 `DataIntegrityViolation` 或任务错误。
- 游客/缓存隔离、五种重复、两种编辑/删除范围、健康联动、通知生命周期与任务/饮水/睡眠提醒 ID 隔离由 Flutter Widget/控制器和后端自动化补足；设备代表性主链路未发现新的阻断问题。
- 通知权限拒绝状态下页面和任务操作保持可用；未改动用户设备的权限选择。

### APK

- 路径：`E:\Healthy-worktrees\tasks-qa\mobile\build\app\outputs\flutter-apk\app-debug.apk`
- 大小：179,874,448 字节
- SHA-256：`AEF07A7C16C8C26232C3F1312E822C139D2E316EB80782468C7AB0EBF67932B0`

### 结论与遗留

- 结论：建议合并，形成唯一 QA 候选；不得推送 `main`。
- 非阻断遗留：缺少 MySQL 8.4 实例，需在发布流水线执行一次目标数据库迁移；Flutter 的 KGP 迁移警告和依赖更新提示留待独立维护任务。
- 环境：验收完成后释放 8080、关闭 API 36 模拟器；最终提交后工作树应干净。
