# 睡眠管理独立验收记录

日期：2026-09-09

QA 分支：`codex/sleep-qa`

独立工作树：`E:\Healthy-worktrees\sleep-qa`

## 提交记录

| 项目 | 提交 |
|---|---|
| 精确基线 | `23c1f901525791b30f7857bde5a960996504ced4` |
| 后端候选 | 等待 `codex/sleep-backend` 完成后记录 |
| Flutter 候选 | 等待 `codex/sleep-flutter` 完成后记录 |
| QA 最终候选 | 待定 |

所有合并和必要修复只进入 `codex/sleep-qa`，不得修改或推送 `main`。提交邮箱固定为 `3040503900@qq.com`。

## 基线结果

以下检查均在未合并睡眠候选的精确基线 `23c1f90` 上执行。

| 检查 | 命令 | 结果 |
|---|---|---|
| 后端全量测试 | `cd backend; .\mvnw.cmd test` | 通过，98 项，0 失败、0 错误、0 跳过 |
| Flutter 全量测试 | `cd mobile; flutter test` | 通过，115 项 |
| Flutter 静态分析 | `cd mobile; flutter analyze` | 通过，无问题 |
| Debug APK | `cd mobile; flutter build apk --debug` | 通过，生成 `mobile/build/app/outputs/flutter-apk/app-debug.apk` |
| 仓库总检查 | `.\scripts\check.ps1` | 通过；包含空白、敏感文件、必需文件和后端全量检查 |
| Android 设备 | `emulator -list-avds` | 已存在 `LightBite_API_36`，最终阶段启动并核验 API 级别 |

基线 APK 为 `E:\Healthy-worktrees\sleep-qa\mobile\build\app\outputs\flutter-apk\app-debug.apk`，大小 `179,706,464` 字节，SHA-256 为 `1CA2ADDC60DA32556824661C42E80B848149D76EB33DFA0750E2983CBF9AAF00`。该值只记录基线，最终候选必须重新构建并重新计算。

非阻断工具链提示：当前 H2 2.4.240 高于 Flyway 已验证版本 2.3.232；11 个 Flutter 包存在与当前约束不兼容的更新；`flutter_timezone` 尚未迁移到 Built-in Kotlin；Gradle 使用未来将收紧的原生访问。均未导致基线失败。

## 自动化验收矩阵

状态说明：`待验收` 表示候选尚未合并；`通过` 必须有独立自动化、真实接口或设备证据；`阻断` 表示不能形成最终候选。

| 领域 | 验收点 | 独立证据 | 状态 |
|---|---|---|---|
| 数据库升级 | V7→V8 保留账号、档案、计划、饮食、饮水和运动数据 | `SleepMigrationTests`、后端全量测试和迁移前后行值断言 | 待验收 |
| 数据库结构 | 两张睡眠表、外键、索引、唯一约束和类型/时长/质量/备注/标签边界 | 迁移测试与 SQL 审查 | 待验收 |
| API 契约 | `/api/v1`、鉴权、查询参数、幂等头、中文统一错误；请求不接受派生字段 | 后端集成测试与 Flutter 请求测试互证 | 待验收 |
| 服务端派生 | 只由开始/结束/时区计算分钟数与稳定醒来日期 | 固定时钟服务测试、真实 H2 请求响应 | 待验收 |
| 类型与边界 | 夜间 30 分钟–16 小时、小睡 5 分钟–4 小时；结束晚于开始；未来容差 5 分钟 | 边界表驱动测试 | 待验收 |
| 跨午夜与时区 | IANA、固定偏移、跨午夜按创建时区醒来日期归档，切换时区不改历史 | 服务与集成测试 | 待验收 |
| 重叠与唯一夜间 | 同用户所有未删除区间不重叠；同醒来日期仅一条夜间记录；编辑排除自身 | 服务与并发集成测试 | 待验收 |
| 质量、标签和备注 | 质量 1–5/可空，七个固定标签去重，备注清理且不超过 500 字 | 服务、数据库和接口边界测试 | 待验收 |
| 幂等 | 串行与并发重试只创建一条；不同用户同键互不影响 | 并发集成测试与唯一约束检查 | 待验收 |
| 用户隔离与软删除 | 查询、编辑、删除不泄露其他账号；重复删除语义一致 | 两账号集成测试 | 待验收 |
| 日汇总 | 按醒来日期返回夜间、小睡、倒序记录和准确时长 | 服务与接口快照测试 | 待验收 |
| 最近 7 天 | 所选日及前 6 日；平均夜间、达标天数、平均质量、小睡总时长和少于 3 条标记 | 固定日期统计测试 | 待验收 |
| 计划状态 | 活动计划目标；无计划、暂停、待重算和风险状态不虚构目标 | 服务与 Today 映射测试 | 待验收 |
| Today 降级 | sleep 失败只返回 `ERROR`，计划、体重、饮食、饮水和运动继续返回 | Today 服务与集成测试 | 待验收 |
| Flutter 模型 | 未知字段兼容，记录/日/7 天/计划状态和质量标签解析 | 数据模型测试 | 待验收 |
| Flutter 请求与幂等 | 读写路径和载荷；失败保留完整草稿并复用创建幂等键 | API 与控制器测试 | 待验收 |
| 缓存隔离 | 按账号＋醒来日期隔离；切换账号/日期不显示旧数据 | 控制器与页面测试 | 待验收 |
| 缓存竞态 | 写入结果不被旧读取覆盖；页面销毁、账号切换丢弃迟到响应 | 可控异步测试 | 待验收 |
| 页面与入口 | 游客、未建档、空/正常/不足/异常/缓存；六项导航不变，Today 两入口正确 | Widget 测试 | 待验收 |
| 表单交互 | 夜间/小睡默认值、原生时间选择、自动时长、质量、标签、备注、编辑和删除 | Widget 测试与设备触控 | 待验收 |
| 提醒隔离 | 一账号一组、至少一天、稳定通知 ID、仅取消本账号睡眠提醒、不影响饮水 | 调度器纯函数与适配层测试 | 待验收 |
| 提醒生命周期 | 仅主动开启请求权限；允许/拒绝、设置入口、时区/启动/恢复重排、退出取消 | 调度器与会话测试、设备触控 | 待验收 |
| 文案与视觉 | 新增可见内容中文；主要卡片、按钮、输入框、弹窗 6 px 圆角 | 源码扫描、Widget 测试和设备巡检 | 待验收 |
| 回归 | 后端、Flutter、格式、静态分析、APK 和仓库总检查 | 最终命令记录 | 待验收 |

## Android API 36 触控矩阵

| 场景 | 预期 | 状态 |
|---|---|---|
| Today 两入口 | 摘要进入管理页；快捷操作进入记录页；不新增底部导航 | 待验收 |
| 跨午夜夜间睡眠 | 保存后按醒来日期归档，管理页与 Today 立即刷新 | 待验收 |
| 小睡 | 可与不重叠夜间记录共存，小睡不计入夜间目标 | 待验收 |
| 质量、标签和备注 | 五级质量、七个标签、备注折叠/展开和 500 字边界可操作 | 待验收 |
| 编辑与删除 | 修改后汇总刷新；删除前显示中文确认 | 待验收 |
| 历史补录 | 可保存历史记录并稳定归档 | 待验收 |
| 重复与重叠 | 第二条同日夜间、区间重叠显示明确中文冲突 | 待验收 |
| 未来与时长边界 | 未来超过容差、短于/长于类型范围均拦截 | 待验收 |
| 7 天统计 | 条形和文字汇总与服务端一致，记录不足状态正确 | 待验收 |
| 游客与未建档 | 分别进入登录和建档，完成后恢复原操作 | 待验收 |
| 断网 | 有缓存标记“数据可能不是最新”；重新加载；重试不重复创建 | 待验收 |
| 提醒时间与星期 | 开关、原生时间选择、至少一个星期、重排可操作 | 待验收 |
| 通知权限允许/拒绝 | 拒绝不影响记录并显示系统设置入口；允许后安排通知 | 待验收 |
| 退出和模块隔离 | 退出取消当前账号睡眠提醒，饮水提醒不受影响 | 待验收 |
| 720×1600 | 核心操作可见可触达，无溢出 | 待验收 |
| 1.3 倍字体 | 核心信息和操作可访问，无不可恢复遮挡 | 待验收 |
| 键盘与安全区 | 底部保存按钮不被备注键盘或系统手势区遮挡 | 待验收 |
| 减少动画 | 功能完整，无强制庆祝动画 | 待验收 |

设备临时设置必须在验收结束后恢复。最终记录模拟器名称、API、分辨率、字体缩放、动画设置、后端启动方式、测试账号状态和每项结果。

## 最终产物

候选合并并完成全部验收后补充：后端和 Flutter 候选哈希、合并/修复提交、最终 APK 的绝对路径、字节大小、SHA-256、遗留项、工作树状态和“可合并/不可合并”结论。
