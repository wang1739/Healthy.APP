# 睡眠管理独立验收记录

日期：2026-09-09

QA 分支：`codex/sleep-qa`

独立工作树：`E:\Healthy-worktrees\sleep-qa`

## 提交记录

| 项目 | 提交 |
|---|---|
| 精确基线 | `23c1f901525791b30f7857bde5a960996504ced4` |
| 验收矩阵与基线 | `3647969c4e17aa3c11e12198be9b3dc47e822ef8` |
| 后端候选 | `40684cef9a1e950e4a497db80ef10f28471af092` |
| 后端合并 | `738bd27` |
| Flutter 候选 | `5ab1bc6849d86d74d37dd60f151784367f5da877` |
| Flutter 合并 | `ca3a7a0` |
| QA 最终候选 | 本文件随最终提交记录，以最终 `HEAD` 为准 |

所有合并和修复仅进入 `codex/sleep-qa`。未修改、未推送 `main`；提交邮箱为 `3040503900@qq.com`。

## 基线结果

候选合并前在精确基线 `23c1f90` 上完成：后端 98 项、Flutter 115 项、`flutter analyze`、Debug APK 和 `scripts/check.ps1` 全部通过。基线 APK 为 `179,706,464` 字节，SHA-256 为 `1CA2ADDC60DA32556824661C42E80B848149D76EB33DFA0750E2983CBF9AAF00`。

## 独立契约验收

| 领域 | 独立证据与结果 | 状态 |
|---|---|---|
| V7→V8 | `SleepMigrationTests` 验证升级保留账号、档案、计划、饮食、饮水和运动数据，并核对两张睡眠表、外键、索引及约束 | 通过 |
| 时间与归档 | 固定时钟测试覆盖夜间 30–960 分钟、小睡 5–240 分钟、结束晚于开始、未来 5 分钟容差、IANA/固定偏移、跨午夜醒来日期和历史时区稳定性 | 通过 |
| 重叠与唯一夜间 | 服务/集成测试覆盖跨类型重叠、同醒来日一条夜间记录及编辑排除自身；真实 H2 请求重叠返回 HTTP 409 | 通过 |
| 幂等与隔离 | 串行及并发重试只创建一条，不同账号同键互不影响；查询、编辑、软删除均按账号隔离 | 通过 |
| 质量、标签、备注 | 质量 1–5/可空、七个固定标签去重、备注清理和 500 字边界通过服务、接口和 Flutter 测试 | 通过 |
| 日与 7 天统计 | 真实 H2 返回醒来日 `2026-09-09`、夜间 `480` 分钟、平均质量 `4.0`、小睡 `30` 分钟、7 日数组长度 `7`；固定日期测试覆盖不足 3 条状态 | 通过 |
| 计划状态 | 无计划、暂停、待重算、风险及有效活动计划均不虚构目标 | 通过 |
| Flutter 契约 | 模型容错、请求路径/载荷、完整草稿、创建幂等键、账号＋日期缓存均通过 | 通过 |
| 缓存竞态 | 可控异步测试验证旧读取不覆盖写入，页面销毁和账号切换会丢弃迟到响应 | 通过 |
| Today 降级 | 睡眠失败仅将睡眠块置为 `ERROR`，其他模块继续返回；设备断网后保留缓存并显示“数据可能不是最新，请重新加载” | 通过 |
| 提醒隔离 | 睡眠通知使用独立稳定 ID，退出只取消当前账号睡眠提醒，不触碰饮水提醒；偏好按账号保留 | 通过 |
| 提醒生命周期 | 主动开启才请求权限，允许/拒绝、设置入口、启动/会话/前台恢复重排及退出取消均通过 | 通过 |
| 中文与圆角 | 新增可见文案为中文，主要卡片、按钮、输入框和弹窗使用 6 px 圆角；Widget 与设备巡检无英文泄漏 | 通过 |

真实 H2 契约矩阵摘要：`idempotent=true`、`wakeDate=2026-09-09`、`duration=480`、`overlapHttp=409`、`averageNight=480`、`averageQuality=4.0`、`napTotal=30`、`enoughTrend=true`、`days=7`。

## QA 最小修复

独立验收发现：应用恢复前台时不会在全局重新安排当前账号的睡前提醒，因此系统时区或时间变化只有进入睡眠页后才生效。`AppShell` 现观察应用生命周期并在 `resumed` 时调用现有调度器；新增 Widget 回归测试先复现、再验证恢复前台后完成重排。未引入新抽象或依赖。

## Android API 36 触控验收

设备：`LightBite_API_36`，Android API 36，最终 APK 实装。

| 场景 | 结果 | 状态 |
|---|---|---|
| Today 与睡眠管理 | 从 Today 睡眠摘要进入管理页；六项底部导航保持不变 | 通过 |
| 跨午夜夜间睡眠 | 触控创建 `23:38–07:38`，服务端归档至醒来日并显示 480 分钟 | 通过 |
| 质量、标签、备注与编辑 | 触控设置质量 5、多个标签并编辑备注 `QA-note`；真实接口值一致 | 通过 |
| 重叠错误 | 创建与夜间记录重叠的小睡，界面显示中文“睡眠时间与已有记录重叠” | 通过 |
| 删除 | 显示中文确认及统计同步说明，确认后记录和汇总刷新 | 通过 |
| 小睡和历史补录 | 触控创建 30 分钟小睡；接口补录历史夜间后，设备显示 8 小时、4.0、30 分钟及对应日条形 | 通过 |
| 通知权限允许 | 系统授权后按所选周一/三/五安排 3 个 22:30 中文通知，ID 为 `312510801/803/805` | 通过 |
| 通知权限拒绝 | 拒绝后不影响记录，显示“通知权限未开启，睡眠记录仍可正常使用。”及系统设置入口，未残留通知 | 通过 |
| 退出和模块隔离 | 退出后睡眠通知清空，账号偏好保留；饮水通知 ID 空间独立 | 通过 |
| 断网缓存 | 停止后端并下拉刷新，缓存的昨晚、7 天及当天记录保留，出现数据非最新提示 | 通过 |
| 720×1600、1.3 倍字体 | 核心信息和操作可滚动、可触达，无横向溢出 | 通过 |
| 键盘、安全区、减少动画 | 备注/登录输入可聚焦和滚动，底部操作未被手势区遮挡；三项动画缩放为 0 时功能正常 | 通过 |

设备设置已恢复为 `1080×2400`、字体缩放 `1.0`、窗口/过渡/动画缩放均 `1.0`、自动旋转设置保持原值。

## 最终回归

| 检查 | 结果 |
|---|---|
| 睡眠后端定向测试 | 34 项通过 |
| 睡眠/应用/Today Flutter 定向测试 | 51 项通过 |
| 后端全量 | 112 项通过，0 失败、0 错误、0 跳过 |
| Flutter 全量 | 137 项通过 |
| Flutter 静态分析 | `No issues found` |
| Dart 格式 | 80 个文件检查，0 个变化 |
| Debug APK | 构建通过 |
| 仓库总检查 | `scripts/check.ps1` 通过 |
| 空白检查 | `git diff --check` 通过 |

## 最终产物与结论

Debug APK：`E:\Healthy-worktrees\sleep-qa\mobile\build\app\outputs\flutter-apk\app-debug.apk`

- 大小：`215,044,321` 字节（`205.082 MiB`）
- SHA-256：`9A4E40E5928B01734040DB4AB80CB2813E9DF0A422894A296AAD1E361733FEB7`

非阻断提示：H2 `2.4.240` 高于 Flyway 已验证版本 `2.3.232`；11 个 Flutter 包存在与当前约束不兼容的更新；`flutter_timezone` 尚未迁移到 Built-in Kotlin；Gradle 对原生访问有未来兼容提示。均未造成测试、分析、构建或设备验收失败。

结论：`codex/sleep-qa` 是唯一 QA 候选，满足计划任务 8，可合并。最终提交后必须保持工作树干净；主线复核和推送属于计划任务 9，本任务未执行。
