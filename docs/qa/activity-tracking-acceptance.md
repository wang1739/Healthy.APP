# 运动管理独立验收记录

日期：2026-09-09  
QA 分支：`codex/activity-qa`  
独立工作树：`E:\Healthy-worktrees\activity-qa`

## 提交记录

| 项目 | 提交 |
|---|---|
| 精确基线 | `af22a576a050ef01ecbdb11f2b60744b5a5f36ab` |
| QA 基线记录 | `944766d6fa299308485f8555dd34dfa292ea65ec` |
| 后端候选 | `273a57b25b3f267ced9520669400215ccea6959d` |
| Flutter 候选 | `8c82ff166de546267e067ca2584234d1ac911279` |
| QA 合并 | 后端 `43f9c58`；Flutter `6ee3b52` |
| QA 最终候选 | 本文所在 `codex/activity-qa` 提交（哈希见交付报告） |

所有合并和必要修复只进入 `codex/activity-qa`，不得修改或推送 `main`。提交邮箱固定为 `3040503900@qq.com`。

## 基线结果

以下检查均在未合并运动候选的精确基线 `af22a57` 上执行。

| 检查 | 命令 | 结果 |
|---|---|---|
| 后端全量测试 | `cd backend; .\mvnw.cmd test` | 通过，80 项，0 失败、0 错误、0 跳过 |
| Flutter 全量测试 | `cd mobile; flutter test` | 通过，94 项 |
| Flutter 静态分析 | `cd mobile; flutter analyze` | 通过，无问题 |
| Debug APK | `cd mobile; flutter build apk --debug` | 通过，生成 `mobile/build/app/outputs/flutter-apk/app-debug.apk` |
| 仓库总检查 | `.\scripts\check.ps1` | 通过；包含空白、敏感文件、必需文件和后端全量检查 |
| Android 设备 | `adb devices -l` | 基线阶段无已连接设备；最终阶段必须使用 API 36 模拟器 |

非阻断工具链提示：当前 H2 2.4.240 高于 Flyway 已验证版本 2.3.232；`flutter_timezone` 尚未迁移到 Built-in Kotlin；Android 命令行工具与 SDK XML 版本存在前瞻性兼容警告。三者均未导致基线失败。

## 自动化验收矩阵

状态说明：`通过` 均有自动化测试、独立命令、真实 H2 接口或 API 36 设备证据；无阻断项。

| 领域 | 验收点 | 独立证据 | 状态 |
|---|---|---|---|
| 数据库升级 | V6→V7 保留账号、档案、计划、饮食和饮水数据 | `ActivityMigrationTests`、后端全量测试 | 通过 |
| 数据库结构 | 两张运动表、外键、索引、唯一约束、边界约束、14 个中文种子 | 迁移测试与 SQL 审查 | 通过 |
| 类型 API/权限 | 搜索、空查询、自定义、重名、无效引用和跨用户隔离 | 服务与集成测试；真实 H2 自定义类型复用 | 通过 |
| API 契约 | 鉴权、查询、幂等头、中文错误、嵌套类型、热量模式、当前体重 | 后端与 Flutter 契约测试互证；真实 H2 流程 | 通过 |
| 热量与快照 | 三档 MET、取整、体重/MET/版本快照、人工覆盖与恢复 | 服务测试、数据库断言、API 36 实时预览 | 通过 |
| 边界校验 | 时长、热量、无体重、未来时间、非法强度 | 服务、接口和 Widget 边界测试 | 通过 |
| 幂等与隔离 | 串行/并发重试、不同用户同键、记录访问隔离 | 并发集成测试；真实 H2 重放只保留一条 | 通过 |
| 删除、日期和周界 | 软删除、跨午夜、IANA/固定偏移、周一至周日 | 服务与集成测试 | 通过 |
| 计划与 Today | 全计划状态；运动故障不阻断其他模块 | Today 服务、集成与 Widget 测试 | 通过 |
| Flutter 模型 | 未知字段、嵌套 `activityType.id`、日/周/记录解析 | 数据模型回归测试 | 通过 |
| Flutter 保存 | 三种 `calorieMode`、失败重试幂等、保存防重复 | API、控制器和 Widget 测试 | 通过 |
| 缓存与竞态 | 账户/日期隔离、迟到读取、销毁页面、离线陈旧标记 | 控制器与页面测试 | 通过 |
| 页面与入口 | 游客、未建档、空/正常/异常；两个真实入口；六项导航不变 | Widget 测试与 API 36 触控 | 通过 |
| 文案与视觉 | 新增可见文案中文；主要控件 6 px 圆角 | 源码、Widget 与设备巡检 | 通过 |
| 回归 | 后端 98、Flutter 115、静态分析、Debug APK、仓库检查 | 最终命令记录 | 通过 |

## Android API 36 触控矩阵

| 场景 | 预期 | 状态 |
|---|---|---|
| Today 快捷入口 | 进入运动管理，不新增底部导航项 | 通过：设备确认六项导航和两个运动入口 |
| 内置运动 | 新增后运动页与 Today 立即显示服务端权威汇总 | 通过：设备新增跑步后两处同步为 105 分钟、1204 kcal |
| 自定义运动 | 创建并能再次搜索使用 | 通过：真实 H2 创建 `QA室内慢跑`，设备搜索复用 |
| 三档强度与时长 | 选择、估算、快捷/手动输入和边界 | 通过：设备触控覆盖高强度、60 分钟和手动输入；其余由 Widget/服务边界测试覆盖 |
| 热量 | 自动估算、人工修改、恢复估算 | 通过：设备显示 259 kcal 实时估算并展开人工输入；真实 API 与回归测试覆盖修改/恢复 |
| 编辑和删除 | 编辑回填、统计刷新、中文删除确认 | 通过：设备确认编辑页回填跑步/高强度/60 分钟；Widget 与 API 覆盖确认和删除 |
| 历史、未来、日期与周进度 | 时间边界及跨日期状态正确 | 通过：固定时钟服务测试和 Widget 测试 |
| 权限闭环 | 游客进入登录，完成档案后恢复真实数据 | 通过：设备从游客引导到密码登录并进入运动管理 |
| 断网 | 缓存陈旧标记，重试不重复新增 | 通过：可控网络异常与幂等回归测试 |
| 720×1600、1.3 倍字体 | 核心操作可见可触达，无溢出 | 通过：设备检查 Today、运动页和录入页 |
| 键盘与安全区 | 底部保存按钮不被输入区域或系统手势区遮挡 | 通过：设备录入焦点下保存按钮保持在 `[42,1401][678,1537]` |
| 减少动画 | 功能完整，无强制庆祝动画 | 通过：三项动画比例为 0 时完成导航和表单触控 |

设备：`LightBite_API_36` / Android API 36。临时使用 720×1600、字体 1.3、三项动画 0；结束后已恢复 1080×2400、字体 1.0、窗口/过渡动画 1.0、动画时长为系统默认。后端以 `test` profile、H2 测试类路径和调试验证码开关启动；测试账号已完成档案，当前体重 62.5 kg。

## 最终产物

最终 Debug APK：`E:\Healthy-worktrees\activity-qa\mobile\build\app\outputs\flutter-apk\app-debug.apk`

大小：`214,921,999` 字节

SHA-256：`0E57D9454CBF620A45C6CE000E97CDCA0BF1291371136C222A09E1F0092CDDA6`

独立 QA 共修复三项候选间契约缺口：记录类型采用嵌套 `activityType.id`；创建/编辑显式发送三种 `calorieMode`；日汇总返回当前 `weightKg` 以支持客户端实时估算。无功能阻断遗留项，可合并。非阻断提示仅为 H2/Flyway 版本、Flutter 可升级依赖与 Kotlin/Gradle 前瞻兼容警告。
