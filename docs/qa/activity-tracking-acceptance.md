# 运动管理独立验收记录

日期：2026-09-09  
QA 分支：`codex/activity-qa`  
独立工作树：`E:\Healthy-worktrees\activity-qa`

## 提交记录

| 项目 | 提交 |
|---|---|
| 精确基线 | `af22a576a050ef01ecbdb11f2b60744b5a5f36ab` |
| 后端候选 | 等待 `codex/activity-backend` 完成后记录 |
| Flutter 候选 | 等待 `codex/activity-flutter` 完成后记录 |
| QA 最终候选 | 待定 |

所有合并和必要修复只进入 `codex/activity-qa`，不得修改或推送 `main`。提交邮箱固定为 `3040503900@qq.com`。

## 基线结果

以下检查均在未合并运动候选的精确基线 `af22a57` 上执行。

| 检查 | 命令 | 结果 |
|---|---|---|
| 后端全量测试 | `cd backend; .\mvnw.cmd test` | 通过，80 项，0 失败、0 错误、0 跳过 |
| Flutter 全量测试 | `cd mobile; flutter test` | 通过，94 项 |
| Flutter 静态分析 | `cd mobile; flutter analyze` | 通过，无问题 |
| Debug APK | `cd mobile; flutter build apk --debug` | 通过，生成 `mobile/build/app/outputs/flutter-apk/app-debug.apk` |
| 仓库总检查 | `.\scripts\check.ps1` | 通过；包含空白、敏感文件、必需文件和后端全量检查 |
| Android 设备 | `adb devices -l` | 基线阶段无已连接设备；最终阶段必须使用 API 36 模拟器 |

非阻断工具链提示：当前 H2 2.4.240 高于 Flyway 已验证版本 2.3.232；`flutter_timezone` 尚未迁移到 Built-in Kotlin；Android 命令行工具与 SDK XML 版本存在前瞻性兼容警告。三者均未导致基线失败。

## 自动化验收矩阵

状态说明：`待验收` 表示候选尚未合并；`通过` 必须有自动化测试或独立命令证据；`阻断` 表示不能形成最终候选。

| 领域 | 验收点 | 独立证据 | 状态 |
|---|---|---|---|
| 数据库升级 | V6→V7 保留账号、档案、计划、饮食和饮水数据 | 运行 `ActivityMigrationTests` 和后端全量测试 | 待验收 |
| 数据库结构 | 两张运动表、外键、索引、唯一约束、边界约束、14 个中文种子 | 迁移测试与 SQL 审查 | 待验收 |
| 类型 API | 系统类型完整、中文搜索、空查询、自定义类型创建 | 服务与集成测试 | 待验收 |
| 类型权限 | 规范化重名、无效参考、他人自定义类型不可见不可引用 | 跨用户集成测试 | 待验收 |
| API 契约 | `/api/v1` 路径、鉴权、查询参数、幂等头、统一中文错误结构 | 后端集成测试与 Flutter 请求测试互证 | 待验收 |
| 热量计算 | 低/中/高 MET、`MET × kg × 小时`、整数四舍五入 | 固定输入输出快照测试 | 待验收 |
| 历史快照 | 体重、MET、计算版本、估算值、最终值和人工覆盖来源持久化 | 创建/编辑后数据库断言 | 待验收 |
| 边界校验 | 时长 1–1440、热量 0–10000、无体重、未来时间、非法强度 | 服务与接口边界测试 | 待验收 |
| 幂等 | 串行与并发重试只创建一条；不同用户同键互不影响 | 并发集成测试和唯一约束检查 | 待验收 |
| 用户隔离 | 查询、编辑、删除和自定义类型均不泄露其他账号数据 | 两账号集成测试 | 待验收 |
| 软删除 | 本人删除生效，重复删除/不存在/跨用户响应不泄露存在性 | 删除接口测试 | 待验收 |
| 日期时区 | UTC 保存、IANA 时区、固定偏移、跨午夜 | 日期查询与写入集成测试 | 待验收 |
| 周边界 | 用户时区周一至周日，同日多条只计一天 | 周一/周日固定时钟测试 | 待验收 |
| 计划状态 | 有计划、无计划、暂停、待重算、风险阻断目标语义正确 | 周汇总测试 | 待验收 |
| Today 降级 | 运动模块异常只返回运动 `ERROR`，其他模块继续返回 | Today 服务与集成测试 | 待验收 |
| Flutter 模型 | 未知字段兼容，日/周/记录/计划状态解析正确 | 数据模型测试 | 待验收 |
| Flutter 幂等 | 新增失败后保留表单并复用同一幂等键 | API 与控制器测试 | 待验收 |
| 缓存隔离 | 按账户和日期隔离，切换账号/日期不闪现旧数据 | 控制器与页面测试 | 待验收 |
| 缓存竞态 | 写入结果不被旧读取覆盖，页面销毁后丢弃迟到响应 | 可控异步测试 | 待验收 |
| 页面状态 | 游客、未建档、空数据、正常、完成、无计划、异常和缓存 | Widget 测试 | 待验收 |
| Today 入口 | 不新增底部导航；快捷入口和摘要卡进入运动管理 | 导航测试 | 待验收 |
| 表单交互 | 类型、自定义、三档强度、快捷/手动时长、时间和热量覆盖 | Widget 测试与设备触控 | 待验收 |
| 编辑删除 | 编辑快照刷新、中文删除确认、保存防重复 | Widget 测试与设备触控 | 待验收 |
| 文案规范 | 全部新增用户可见文案为中文 | 源码扫描、Widget 测试和设备巡检 | 待验收 |
| 视觉规范 | 新增主要卡片、按钮、输入框、弹窗均为 6 px 圆角 | 源码扫描、Widget 测试和设备巡检 | 待验收 |
| 回归 | 后端全量、Flutter 全量、静态分析、Debug APK、仓库总检查 | 最终命令记录 | 待验收 |

## Android API 36 触控矩阵

| 场景 | 预期 | 状态 |
|---|---|---|
| Today 快捷入口 | 进入运动管理，不新增底部导航项 | 待验收 |
| 内置运动 | 新增后运动页与 Today 立即显示服务端权威汇总 | 待验收 |
| 自定义运动 | 中文搜索无结果后创建，并能再次搜索使用 | 待验收 |
| 三档强度 | 低、中、高均可选择并影响估算 | 待验收 |
| 快捷时长 | 15、30、45、60 分钟均可保存 | 待验收 |
| 手动时长 | 键盘输入 1–1440，越界显示中文错误 | 待验收 |
| 热量 | 自动估算、人工修改、恢复估算均正确 | 待验收 |
| 编辑和删除 | 编辑刷新统计；删除前显示中文确认 | 待验收 |
| 历史与未来 | 允许历史补录；未来时间在客户端和服务端拦截 | 待验收 |
| 日期与周进度 | 跨日期无旧数据闪现，周进度符合服务端汇总 | 待验收 |
| 权限闭环 | 游客进入登录，未建档进入建档，完成后恢复原操作 | 待验收 |
| 断网 | 有缓存时标记“数据可能不是最新”，重试不重复新增 | 待验收 |
| 720×1600 | 核心操作可见可触达，无溢出 | 待验收 |
| 1.3 倍字体 | 核心信息和操作可访问，无不可恢复遮挡 | 待验收 |
| 键盘与安全区 | 底部保存按钮不被键盘或系统手势区遮挡 | 待验收 |
| 减少动画 | 功能完整，无强制庆祝动画 | 待验收 |

设备临时设置必须在验收结束后恢复。最终记录模拟器名称、API、分辨率、字体缩放、动画设置、后端启动方式、测试账号状态和每项结果。

## 最终产物

候选合并并完成全部验收后补充：后端和 Flutter 候选哈希、合并/修复提交、最终 APK 的绝对路径、字节大小、SHA-256、遗留项、工作树状态和“可合并/不可合并”结论。
